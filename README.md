## For developer
### How to build
##### **UNIX**
1. Install pyinstaller `pip3 install pyinstaller`
2. Elevate privileges `chmod +x build.sh`
3. Run `./build.sh`

##### **Windows**
1. Install pyinstaller `pip3 install pyinstaller`
2. Run `build.bat`